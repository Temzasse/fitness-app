import React from 'react';
import styled from 'styled-components';
import { Text } from '../components/common';
import { getRandomPhotos } from '../utils/photos';

const Catalog = () => {
  return (
    <div>
      <Text variant="title-1">Exercises</Text>
    </div>
  );
};

export default Catalog;

import React from 'react';
import styled from 'styled-components';
import { Text } from '../components/common';

const Profile = () => {
  return (
    <div>
      <Text variant="title-1">Profile</Text>
    </div>
  );
};

export default Profile;
